# Aevum ランタイム依存（モジュール追加に応じて拡張する）
# Python 3.10 固定（hyperliquid-python-sdk の制約）
numpy>=1.26
polars>=1.0                # PC側 特徴量バルク計算（features）
torch>=2.2                 # 学習（dataset/transformer/train）。学習PCはCUDA版を導入すること
onnx>=1.16                 # ONNX エクスポート（train）
onnxscript>=0.1            # torch>=2.x の ONNX エクスポータが要求
onnxruntime>=1.17          # 推論（Pi は ARM 最適化版を導入すること）
asyncpg>=0.29              # TimescaleDB 非同期ドライバ（ingestion）
hyperliquid-python-sdk>=0.9  # WebSocket 購読（ingestion）
fastapi>=0.110             # 状態配信API（server）
uvicorn>=0.29             # ASGI サーバ（server）
